<resources>

    <!-- Base application theme. -->
    <style name="AppTheme_old" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Light blue theme. -->
    </style>
    <style name="Our_Custom_Theme" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:textStyle">normal</item>
        <item name="android:textViewStyle">@style/AppTheme</item>
        <item name="android:fontFamily">Robato</item>
        <item name="android:windowBackground">@drawable/bg_gradient</item>
        <item name="android:colorBackgroundCacheHint">#00000000</item>
    </style>

    <style name="posible_heme" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:textColor">@color/abc_search_url_text_holo</item>
        <item name="android:textStyle">normal</item>
        <item name="android:fontFamily">Robato</item>
        <item name="android:windowBackground">@drawable/abc_ab_bottom_solid_dark_holo</item>
        <item name="android:colorBackgroundCacheHint">@color/bg</item>
    </style>

</resources>

